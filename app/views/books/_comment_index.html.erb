
<% book.book_comments.each do |book_comment| %>
<table>
   <thead>
      <tr>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <tr>
          <td><%= link_to user_path(book_comment.user) do %>
               <%= attachment_image_tag book_comment.user, :profile_image, format: 'jpeg', fallback:'img/no_image.jpg', size: "40x40" %><br>
               <%= book_comment.user.name %>
              <% end %>
          </td>
          <td><%= book_comment.comment %></td>
          <td><%= link_to "Destroy", book_book_comment_path(book_comment), class:"btn btn-sm btn-danger", method: :delete %></td>
      </tr>
        <% end %>
    </tbody>
  </table>
